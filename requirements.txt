# Core — the only hard dependency for the engine itself.
requests>=2.31

# Default LLM provider. Swap/add as you like (see docs/06-providers.md).
anthropic>=0.40
# openai>=1.40

# Convenience for loading the .env file in the examples.
python-dotenv>=1.0
